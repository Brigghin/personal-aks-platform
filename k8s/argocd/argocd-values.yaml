server:
  ingress:
    enabled: true
    ingressClassName: nginx

    hostname: argocd.brianbargholz.ca

    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod

    tls: true

configs:
  params:
    server.insecure: true