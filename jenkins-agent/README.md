# Jenkins Agent

This directory contains the Dockerfile used to build the custom Jenkins agent image for the personal AKS platform.

The image provides a consistent build environment for Jenkins pipelines and includes the tools required to support infrastructure automation, container image management, and Azure-related operations.